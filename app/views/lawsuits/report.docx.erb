<html>
  <body>
    <h1>Rapport</h1>
    <p>Ärende: <%= @lawsuit.name %></p>
    <em>Specifikation avseende arbete:</em>
    <% @price_categories.each do |pc| %>
      <h2 class="strong"><%= pc.name %></h2>
      <table>
        <thead>
          <tr>
            <th>Datum</th>
            <th>Notering</th>
            <th>Arbetad tid</th>
          </tr>
        </thead>
        <tbody>
          <% pc.tasks.where(lawsuit_id: @lawsuit.id).each do |task| %>
            <tr>
              <td><%= task.date %></td>
              <td><%= task.entry %></td>
              <td><%= task.worked_hours %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <p class="right">-----------------------------------</p>
      <p class="right"><%= pc.tasks.where(lawsuit_id: @lawsuit.id).sum(:worked_hours) %></p>
    <% end %>
  </body>
</html>
